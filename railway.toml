# Railway Configuration
# Configuración para despliegue en Railway

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "node server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Health check
healthcheckPath = "/health"
healthcheckTimeout = 100
healthcheckInterval = 10

# Variables de entorno recomendadas (configurar en Railway dashboard)
# NEXT_PUBLIC_API_BASE_URL=https://apiauthgames-production.up.railway.app
# API_BASE_URL=https://apiauthgames-production.up.railway.app
# NEXT_PUBLIC_APP_URL=https://tu-app.railway.app
# NODE_ENV=production

